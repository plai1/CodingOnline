# very similar to lists but have immutabililty (cannot be mutated)
# use praenthesis: (1, 2, 3)
# good source of data integrity, elements don't get flipped/reassigned

tupl = (1, 2, 3)

print(tupl[0])
print(type(tupl))
print(len(tupl))
